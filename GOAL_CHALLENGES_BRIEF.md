# 碳水大王：成就系统改造为目标挑战（V1 冻结草案）

## 产品决定

- 不做旧成就系统的硬删除，而是改造其产品机制：移除被动自动授勋的用户路径，改为用户主动创建并追踪的目标挑战。
- 在原“成就”卡片位置改为“目标挑战”。挑战由用户主动创建，完成后进入已完成记录。
- 复用旧系统中有价值的能力：可计算的成就条件改造成推荐挑战模板，完成反馈改造成一次性挑战庆祝框。
- 旧成就数据保留兼容读取能力，不能影响已有备份或其他记录；旧的已解锁状态不自动变成进行中挑战。
- V1 覆盖训练、饮食/喝水、身体三类挑战；只接入已有可靠记录来源的数据，缺失记录不得被伪造成完成。

### 核心原则：主动追踪，而非被动解锁

- 推荐挑战只是可选模板，用户必须主动点击创建；系统不得因历史记录自动替用户生成挑战。
- 用户决定是否追踪、追踪什么、目标值和期限；应用只负责透明地计算和展示进度。
- 进行中卡片只显示用户已创建的挑战。没有挑战时显示空状态，不自动塞入三条“默认任务”。
- 完成后保留可回看的挑战记录；对用户主动创建的挑战，首次完成时弹出一次庆祝框，沿用旧成就的正向反馈感，但内容展示挑战名称、最终进度和完成时间，而不是被动授勋。

## 进行中挑战卡

- 标题：`目标挑战`；副文案：`设定目标，持续推进`。
- 右上角三个独立图标按钮：新建挑战、已完成挑战、删除挑战。
- 进行中区域固定为三条赛道：饮食挑战、训练挑战、恢复挑战；按赛道顺序显示，而非按进度排序。
- 同时最多只能有三项进行中挑战，并且每个赛道最多一项；赛道空缺时显示对应的新建引导，不允许在同一赛道重复创建第二项。
- 三项上限只限制“进行中挑战”的展示和主动管理，不限制底层训练、饮食/饮水、恢复和身体指标的持续统计。
- 每条使用与碳水/蛋白/脂肪一致的进度条样式，至少显示：挑战名称、当前值/目标值、进度百分比、剩余/已用时间（有期限时）。
- 三个赛道都为空时显示总空状态；单个赛道为空时保留该赛道的新建引导，不使用旧成就插画或被动奖励文案。

## 挑战等级与推荐解锁

- 沿用旧成就的阶梯感，但改用新的主动挑战等级，从低到高固定为：绿色“优秀” → 蓝色“精良” → 紫色“史诗” → 橙色“传说”。
- 推荐挑战必须属于某一条挑战链；同一链的下一等级只在上一等级挑战完成后才可推荐。
- 初次进入“推荐挑战”只显示绿色优秀模板；不展示锁定的蓝色、紫色或橙色模板，也不以灰色卡片制造被动解锁压力。
- 完成绿色优秀后，才显示该链的蓝色精良推荐；完成蓝色后才显示紫色；完成紫色后才显示橙色。
- 推荐卡、进行中卡和已完成记录都显示等级名称与对应颜色；颜色只表达难度/阶段，不影响进度计算。
- 自定义挑战不参与等级链，默认标记为“自定义”，使用浅金背景、金色图标/边框；金色不代表传说等级，可独立创建和完成。

## 管理交互

### 新建挑战

- 点击进入全屏“新建挑战”页，不使用小弹窗。
- 顶部有“推荐挑战 / 自定义挑战”两个页签。
- 推荐挑战按分组卡片展示，首次提供：
  - 新手起步：本周期完成 5 次训练、连续训练 3 天；
  - 力量与增肌：30 天累计训练容量、累计动作次数、单动作最大重量；
  - 饮食与补水：连续 7 天饮水 ≥ 2000 ml、连续 7 天达到蛋白质目标、连续 7 天按碳循环目标达标；
  - 身体目标：体重达到目标值、体脂达到目标值、围度达到目标值；
  - 趣味挑战：早间训练 10 次、晚间训练 10 次（仅在训练记录具备可靠时间戳时启用）。
- 每个推荐类别要有递进目标链。例如饮水链：优秀“连续 7 天饮水 ≥ 2000 ml” → 精良“连续 14 天” → 史诗“连续 30 天” → 传说“连续 90 天”；其他类别按相同原则配置递进难度。
- 推荐卡可显示基于历史记录计算出的背景进度，即使该模板尚未被创建为进行中挑战；背景统计本身不弹庆祝、不自动创建挑战。
- 自定义页先选挑战类型，再显示对应字段；公共字段：名称（必填）、挑战宣言（可选）、开始/结束日期（默认从今天起 30 天）。

### V1 挑战类型

1. 训练总容量：全部训练累计重量，支持 kg / lbs。
2. 单动作最大重量：必须选择一个动作，支持 kg / lbs。
3. 完成训练次数：在周期内完成的训练会话数。
4. 连续训练天数：按自然日连续完成训练。
5. 动作总次数：在周期内记录的总重复次数。
6. 连续饮水达标：连续 N 天饮水量大于等于目标 ml；例如“连续 7 天喝水 2000 ml”。某天缺记录或未达标即中断连续计数。
7. 连续饮食达标：连续 N 天达到蛋白质目标或当前碳循环日的碳水/蛋白/脂肪目标；具体允许误差和“达标”口径在开发冻结时明确，缺失餐饮记录不得算达标。
8. 身体数值目标：体重、体脂或六项支持围度达到用户选择的上限/下限目标。只以真实测量记录判断，不补零、不推断。

### 三条赛道归属

- 饮食挑战：蛋白质/碳循环目标达标、饮食相关连续达标目标。
- 训练挑战：训练总容量、单动作最大重量、训练次数、连续训练天数、动作总次数及其推荐等级链。
- 恢复挑战：连续饮水达标、休息/恢复类目标，以及体重、体脂、围度等身体状态目标。
- 用户创建或选中目标时必须先确定赛道；已有同赛道进行中挑战时，提示先完成或删除现有挑战，不得静默覆盖。

### 历史进度同步

- 应用持续保留所有原始记录，并对推荐模板计算背景进度；未选中的目标不会丢失已有进度。
- 用户创建挑战时，必须立即按挑战类型和所选时间范围重算已有历史，而不是从创建时刻一律归零。
- 连续达标类使用截至当前日期的真实连续天数。例如用户已连续 7 天饮水 ≥ 2000 ml，之后创建“连续 7 天饮水 ≥ 2000 ml”，应直接显示 `7/7`。
- 若同步后的进度已达到目标，创建时直接标记为完成并触发一次挑战庆祝；不得要求用户重复完成已真实达成的记录。
- 累计类和身体数值类按用户在新建页选择的开始/结束日期或历史窗口计算；日期外数据不计入。

### 自定义字段补充

- 连续达标类必须选择：指标、每日阈值、连续天数；水量单位固定 ml。
- 身体数值类必须选择：指标、目标方向（不高于 / 不低于）、目标值和单位。
- 训练总容量与最大重量保留 kg / lbs；身体围度使用 cm，体脂使用 %。

### 已完成挑战

- 点击“已完成挑战”进入可滚动的已完成列表。
- 每项保留名称、类型、最终值/目标值、完成时间和挑战周期；只读，不再参与进行中排序。

### 删除挑战

- 点击删除按钮进入删除模式；每条进行中挑战进度条前显示多选圆圈。
- 可多选，仅删除进行中挑战；再次点击“删除已选”必须二次确认，并显示数量。
- 删除后不可恢复；已完成挑战不在本入口删除。

## 数据与状态

- 创建后自动持久化；每次训练、饮食/饮水或身体记录变化时重新计算相关进行中挑战。
- 同时刷新推荐模板的背景进度；三项进行中上限不得停止其他模板的背景统计。
- 达到目标时自动标记完成，写入完成时间和最终进度，并从进行中移入已完成。
- 完成迁移与庆祝状态必须原子保存；同一挑战的庆祝框只出现一次，应用重启或再次查看已完成记录不得重复弹出。
- 老版本的旧成就数据不迁移为挑战，也不再显示；读取旧备份时应安全忽略遗留成就键，不影响训练、饮食和个人资料。

## 验收门槛

- 旧成就入口、卡片和被动解锁逻辑在用户路径中替换为主动目标挑战；用户主动完成挑战时仍保留庆祝反馈。
- 最多三条进行中挑战，饮食/训练/恢复各一条；赛道顺序固定，无第四条滚动列表。
- 训练、饮食/饮水、身体三类 V1 类型的创建、进度计算、自动完成、已完成查看、进行中多选删除均可复现。
- 新建页、完成页、删除模式在 `360x800` 和大字体下无标签换行顶歪、无横向越界，底部保存/确认可达。
- 写入型测试使用隔离 `CARBS_KING_DATA_DIR`；真实网页预检仅作为 iQOO 11S 几何模拟，不标为真机通过。
- 未经用户明确确认，不构建 APK、不推送、不发布。
