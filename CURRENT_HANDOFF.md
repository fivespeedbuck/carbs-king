# 当前交接：以工作区为准

更新时间：2026-07-28。此文件描述当前活动开发；若与 `git diff` 冲突，以 `git diff` 为准。

## 活动来源

- 正在跑开发流程的会话：`说话`。
- `说话 (2)`、`碳水大王 (2/3/4)` 和本会话是历史参考，不应覆盖活动会话或工作区状态。

## 当前批次：围度记录重构（代码已完成，尚未统一验收）

用户已确认的目标：

1. 数据页“记录维度/围度”改成一次填写胸围、腰围、臀围、上臂围、大腿围、小腿围。
2. 同一次保存写入同一日期的一个测量会话；允许只填已测项目，空字段不得把历史数据写成 0 或覆盖掉。
3. “我”页不再直接铺开围度输入框；身高、年龄下面只放“身体围度”按钮，点击后查看/展开。
4. 删除“我”页重复的“记录本次测量”入口；围度记录只从数据页完成。
5. 删除围度时按当天整次测量删除，而非只删一个部位。

### 已在工作区出现的实现

- `src/daily_record_controller.py`
  - 新增 `update_circumferences()`：一次保存多项围度，保留未填旧值。
  - 新增 `delete_circumferences()`：按日期删除整次围度记录。
- `src/data_record_controller.py`
  - 单项围度下拉表单已改为六项同屏的两列输入。
  - 保存时逐项校验 1–300 cm，至少一项；保存提示显示实际记录数量。
  - 删除按钮已改为删除当天围度记录。
- `src/profile_controller.py` 与 `src/profile_details_views.py`
  - “我”页已移除直接输入围度与“记录本次测量”。
  - 身高、年龄下方改为“查看身体围度 / 收起身体围度”，仅展示只读摘要。
- `tests/test_main_analytics_integration.py` 与 `tests/test_profile_macro_and_measurements.py`
  - 已更新静态集成契约，覆盖围度入口迁移与批量保存调用。

### 尚未确认/必须完成

- 为新增多项保存和整次删除补控制器/界面测试，覆盖：全填、部分填、空值、非法值、保留旧值、删除整次、历史兼容。
- 用隔离数据实际验证数据页表单在手机宽度和大字体下的两列对齐、日期写入、趋势刷新。
- 确认旧的单项 `update_circumference()` / `delete_circumference()` 是否仍被其他入口调用；保留兼容或迁移调用，不能盲删。
- 当前 README / CHANGELOG / pyproject 的 v71/Build72 改动是未提交状态的一部分；围度批次完成前不要擅自发布为 v72。

### 当前验收状态（2026-07-28）

- `profile_circumference_expanded` 首次点击白屏的代码根因已修复并同步到正式目录；专项测试、相关 `py_compile` 与 `git diff --check` 已通过。
- 网页 UI 预检被环境阻断：正式目录预览进程虽出现，但 `http://127.0.0.1:8765/` 无监听、连接失败，无法执行真实点击；Flet/Python 进程及 `8765/8766` 已清理。此结论不等于代码修复失败。
- 因未获得真实点击证据，围度批次仍不得标记最终验收完成、不得构建或发布；后续在稳定预览环境或经用户同意的 APK 手机验收中补验。

## 已知暂停项

- 用“主动目标系统”取代被动成就系统：产品方向已讨论，但暂缓，未开始可靠实现。
- `v71` 器械筛选热修复的发布状态必须在下次发布前按远端实际 Release 再确认。

## 待入队需求（非 P0）

- 【当前小批次】移除个人资料的伪默认值：性别、体重、体脂、身高、年龄、运动习惯等首次使用不得预填为具体个人信息；未设置时应明确显示为空/未设置。相关计算与展示不得继续静默使用假默认值，需提示用户完善资料。目标是分享或交给他人使用时不携带看似真实的个人数据。
- 碳循环目标分档：现有“自动计算”宏量目标以减脂为默认基线，新增互斥的“减脂 / 保持 / 增肌”三个按钮；仅在选择自动计算时显示。首次无记录的个人资料弹窗也要在“运动习惯”下增加标题“碳循环目标”和同一组三按钮。选择不同目标后，自动按对应策略计算碳水、蛋白、脂肪；已有用户迁移默认减脂，手动模式不得被强制改写。批次冻结前需先审计并明确现有减脂公式与保持/增肌的计算参数、数据迁移和验收样例。
- 成就系统改造为目标挑战：不硬删除旧系统，而是把被动自动解锁改为用户主动创建、追踪并完成的挑战；原成就卡位置改为“目标挑战”，可复用的旧成就条件转为推荐模板，完成反馈保留为一次性挑战庆祝框。推荐挑战有等级链：绿色优秀 → 蓝色精良 → 紫色史诗 → 橙色传说 → 红色精锐；初始仅显示绿色，完成后才显示该链下一等级。进行中最多三项，允许多项属于同一饮食、训练或恢复赛道；自定义挑战使用金色且不参与等级链。三项上限不限制所有模板的背景统计：未选中的饮水、饮食、训练和身体目标仍按真实记录累计，用户创建时同步历史进度，已达成可直接完成并庆祝。右上角提供新建、已完成、删除三按钮。新建页包含推荐/自定义，V1 覆盖训练、饮食/喝水、身体三类：训练总容量、单动作最大重量、训练次数、连续训练天数、动作总次数、连续饮水/饮食达标、体重/体脂/围度目标；已完成可查看历史，进行中支持多选二次确认删除。成功只在用户点击待确认卡后弹窗，只有精锐完成播放训练完成音频；失败点击失败卡后弹出灰色嘲讽窗口且不播放音频。详细冻结草案见 `GOAL_CHALLENGES_BRIEF.md`。

## 当前工作区快照

截至本交接，已修改或新增：

- `CHANGELOG.md`
- `README.md`
- `pyproject.toml`
- `src/daily_record_controller.py`
- `src/data_record_controller.py`
- `src/training_controller.py`
- `PROJECT_CONTEXT.md`（本次新增）
- `CURRENT_HANDOFF.md`（本次新增）

其中 `training_controller.py` 是 v71 动作库器械展开布局修复；围度批次不要随意回退或覆盖它。

## 最近可引用的验证基线

- 历史会话曾报告 `324 passed / 410 subtests passed`，用于 v70/v71 相关功能。
- 围度批次开始后尚未见完整回归结果；不得把历史通过结果当作当前批次已验证。
- Flet 手机端真实验收不可省略，尤其是围度全屏表单和“我”页入口去重。
