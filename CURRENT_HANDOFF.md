# 当前交接：Build 90 动态碳循环正式候选

更新时间：2026-08-02

## 当前结论

- Build 90 已从参考算法进入正式应用接线阶段，不再制作并存 beta 包；正式 APK 只在自动化、资源、签名和用户验收全部通过后发布。
- 自动宏量模式唯一使用 `src/dynamic_carb_engine.py`，由 `src/dynamic_carb_adapter.py` 统一整理训练事实、生成版本化快照，再由首页、饮食页和训练页消费同一结果。
- 自定义宏量模式完全绕过动态引擎。用户设置的三档宏量倍率是最终目标，训练计划和训练结果不得覆盖；界面只可提示当前使用自定义目标。
- 旧的部位系数、统一负荷分、`0–6 / 7–14 / 15+` 阈值、习惯训练时间和训练前两小时分支已从现行代码删除。
- 31/60/90 天长期能量校准继续以 shadow 方式记录候选值，不静默应用；真实长期饮食与体重数据足够后再开放受限应用。

## Build 90 已接入

- 未决定、明确休息、计划待确认、计划已确认、训练中、已完成、结果未知和手动模式使用明确的数据语义；未知不等于休息，自重不等于 0 kg。
- 未选择训练或休息时暂定低碳；明确休息为低碳；选择训练后仍暂定低碳，直到动作、组数、次数和负重类型通过统一参数校验并确认。
- 首页“今天准备训练吗？”支持休息与训练切换；从休息改为训练会清除旧休息状态并直接进入动作添加。
- 训练安排只允许按住移动手柄排序，普通上下滑动不会拖动卡片；新增“一键确认全部参数”。
- 动作库显示同一动作上次完成的组数、次数和重量/自重，并可作为本次预填；预填仍需确认。
- 首页今日摄入和饮食页饮食总览共用简化详情，只显示目标、已摄入和必要提醒，不暴露负荷分、阈值、窗口系数或计算公式。
- 自动模式手动切换碳档时，若偏离训练建议会先确认；自定义宏量模式不受动态推荐拦截。
- 食物备份使用用户差分，只导出自定义、修改和删除项，不导出内置 2000+ 食物库。
- 历史展示目标使用不可变快照；补录、删除和恢复历史训练只重算实际账本，不改写过去已经展示的目标。

## 验证状态

- 全量测试：489 项通过，443 个子测试通过。
- `compileall` 与 `git diff --check` 通过。
- 隔离 Web 预览已完成基础资料、首页暂定低碳和“今天准备训练吗？”入口检查；未发现运行异常。
- Build 90 正式候选 APK 已构建并通过资源、原生能力、包信息和签名门禁；尚待用户真机验收，因此当前不得发布。
- 产物：`build/apk/carbs_king.apk`，201,847,987 字节，SHA-256 `BA91D2A7A3F763A9C2DBB153DC81E59BC8707F6BA4CACDEC035AE9C8D6C24FBD`。
- 包信息：`com.chenyang.carbs_king`，versionName `1.2.3`，versionCode `90`，`arm64-v8a / armeabi-v7a / x86_64`。
- v2 签名证书 SHA-256：`172A8B5C7A909A79FB483F83CF9FEA71FE6567937C900C1D47EAF0FD67AD75CE`，与现有正式包连续；构建脚本已把源码预备为 Build 91。

## 下一步

1. 交给用户真机验收；只有验收通过后才更新 GitHub Release。
2. 验收重点：自定义宏量不被覆盖、休息改训练、参数确认与一键确认、动作列表滑动/排序、历史参数、训练中追加动作。
3. 本轮完成后单独测量启动耗时，再决定是否清理真正影响启动的冗余路径。

## 项目边界

- 当前唯一使用者和测试者是项目所有者；开发完成前数据可重建或迁移，只维护最新正确结构，不为不存在的旧客户端保留双轨实现。
- 根目录 `gif/` 是用户保留的动作素材来源，不提交、不删除、不进入 APK。
- 训练结束 5 点状态评分及恢复/休息建议属于独立扩展，不直接换算碳水。
