# 下一会话交接：动态碳循环先模型、后代码

更新时间：2026-08-03
工作目录：`D:\carbs-king`
当前分支：`codex/build90-dynamic-carb`

> 历史交接：V3.3-final 已于 2026-08-03 获用户批准入码，正式实现与独立 P0 审计已完成；当前事实以 `CURRENT_HANDOFF.md`、Obsidian 当前状态和工作区为准。本文保留模型先行过程，不再作为“禁止改代码”的当前门禁。

## 用户已经明确的产品决策

1. 目标是干净增肌，不接受每天都处在同一个持续增肌盈余，再靠后续减脂收尾。
2. 低碳/休息日必须同时降低总热量；不能用把脂肪从约 60 g 直接推到约 90 g 的方式，把全部碳水缺口填平。
3. 蛋白质在同一目标阶段保持固定；训练需求主要通过碳水变化表达。
4. 脂肪应保持在有证据支持的可行范围内，具体锚点和档位差必须先经过文献、计算和长期回放，不能凭感觉定值。
5. 用户自定义宏量目标时，自动模型完全旁路；手动目标优先，系统只能提示推荐差异。
6. 任何新公式都必须先完成模型规格和证据审查，再改代码、UI 或构建 APK。没有定稿不得创建 Build 94。

## 当前工作区事实

- 正式发布仍是 `1.2.3 / Build 89`。
- Build 93 只是一份旧的个人测试候选，尚未真机验收；由于能量/脂肪分配模型正在替换，当前不可安装、不可发布、不可作为新模型验证包。
- 之前误写入的 `CK-DCE-v3-calorie-cycle` 求解器已用 Build 93 基线恢复；当前 `src/dynamic_carb_engine.py` 与恢复基线一致。
- 恢复后定向回归：`50 passed, 8 subtests passed`（动态引擎与适配器）；当前工作区全量回归：`521 passed, 448 subtests passed`。此前 Build 93 的 520/448 只代表旧候选，不能替代新模型回放。
- 根目录 `gif/` 是用户素材，必须保留；不要清理或提交。
- 不要使用 `git reset --hard`、`git checkout --` 或覆盖整个工作树。

## 新会话必须按此顺序执行

### A. 只做模型，不改业务代码

1. 读取 Obsidian 启动层、项目导航、当前状态、下一阶段规划、关键决策和动态碳循环规范。
2. 核对现有文献原文与适用范围，至少覆盖：成人能量估计、Atwater 4/4/9、脂肪 AMDR、蛋白范围、自然健美增肌能量盈余、能量赤字与瘦体重、力量训练碳水证据、长期体重/饮食校准。
3. 写出候选 V3 规格，明确区分：文献直接值、数学恒等式、产品推导、工程门槛和未知项。候选至少要说明：
   - 低/中/高需求对应的能量锚点；
   - 固定蛋白的计重口径及体脂缺失/过期降级；
   - 稳定脂肪锚点或受限脂肪范围；
   - 碳水残差公式、热量闭合和取整；
   - 干净增肌、保持、减脂三种目标如何分配总热量；
   - 训练碳水最低需求与目标热量冲突时的处理；
   - 31/60/90 天（必要时 14 天）校准仍如何处理缺失、滞后和 shadow；
   - 手动/自定义旁路、快照和历史回放语义。
4. 用真实备份、100 天 fixture、多画像、缺失饮食/训练、稀疏体重、同日多场和边界数据做“只读回放”。重点观察：普通四/五练是否被普遍判高碳、低碳日脂肪是否异常升高、训练日是否被压到不可执行碳水、能量闭合和未来数据泄漏。
5. 让独立审查者只审模型和证据；修订后再更新唯一规范 [[动态碳循环计算系统]]、[[关键决策]]、需求账本、当前状态和下一阶段规划。

### B. 模型获得明确结论后，才进入代码

1. 先把算法版本、参数集版本和迁移策略写入 Obsidian。
2. 再改 `src/dynamic_carb_engine.py` 及适配器/快照，不先改 UI。
3. 先跑引擎、适配器和全部回放；通过后才接 UI 三档显示、训练卡提示、进度条等待办。
4. 完成全量测试、APK 门禁和真机回归后，才决定是否构建/保留 Build 94；用户未授权前不发布。

## 当前不要做的事

- 不要直接把 `60 g → 90 g` 脂肪规则写回代码。
- 不要把“低碳高脂能维持激素”或“固定热量一定更利于增肌”当作公式依据；现有文献不能支持这种绝对表述。
- 不要用部位、绝对重量、统一负荷分或 7/31 天乘数重新发明训练评分。
- 不要把模拟回放当成人体疗效证据。
- 不要安装或发布 Build 93，也不要因为 `src/app_version.py` 已预备 Build 94 就立即构建。

## 相关入口

- 算法唯一规范：`D:\obsidian\obsidian\02-项目\碳水大王\10-产品与架构\动态碳循环计算系统.md`
- 当前状态：`D:\obsidian\obsidian\02-项目\碳水大王\00-当前\当前状态.md`
- 下一步：`D:\obsidian\obsidian\02-项目\碳水大王\00-当前\下一阶段规划.md`
- 用户需求：`D:\obsidian\obsidian\02-项目\碳水大王\00-当前\需求\需求总览.md`
- 版本账本：`D:\obsidian\obsidian\02-项目\碳水大王\00-当前\版本\版本记录.md`
- 当前交接历史：`D:\carbs-king\CURRENT_HANDOFF.md`
