# 测试协作总结

## 小 P0 快速路径

- 现象：单点白屏、`KeyError`、表单按钮失效等小范围 P0 需要快速闭环，避免重复启动和范围扩张。
- 根因：多角色并行、额外 worktree 或反复尝试预览会增加状态分叉，拖慢定位并留下测试进程。
- 处理：仅允许一名开发负责人直接在 `D:\\carbs-king` 做最小修复；一名测试负责人执行相关专项与必要的 `py_compile`；一名 UI 负责人使用已初始化的隔离 `CARBS_KING_DATA_DIR` 走一条最短真实点击路径。
- 验证：预览启动与点击验收总时限 10 分钟；成功必须报告截图和结果；失败必须停止服务、清理端口，并明确标记“环境阻断”上报。
- 预防：不新建额外 worktree，不多角色重复启动预览，不做范围外优化；只有首次修复失败或根因不清时，才升级为复杂 P0 和多负责人并行。

## 围度展开白屏 P0 复盘（2026-07-28）

- 触发条件/前置数据：在“我”页点击“查看身体围度/当前围度”首次展开；复现前使用隔离 `CARBS_KING_DATA_DIR`，分别准备 `profile_inited=true` 且无历史围度、以及含完整六项历史围度的数据集。不得使用默认用户数据目录。
- 最短复现步骤：从项目根目录启动 Flet Web；进入“我”页；点击“查看身体围度”；观察首次展开是否白屏，再点击收起并刷新页面。原始故障发生在首次展开阶段，初始页面加载并非同一故障。
- 根因或阻断原因：原始故障首个异常为 `KeyError: 'profile_circumference_expanded'`；`AppState` 映射未声明该临时 UI 状态，读取可走默认值但写入触发 `_bindings()` 的 `KeyError`，Flet 页面随后白屏。正式目录专项修复已通过；本轮真实 UI 未能进入业务页面，实际阻断是首次个人资料初始化弹窗，无法走到“我”页入口，未获取新的业务异常堆栈。
- 修改/检查文件：开发修复检查 `src/app_state.py`、`src/profile_controller.py`、`src/profile_details_views.py`；专项测试检查 `tests/test_profile_macro_and_measurements.py`、`tests/test_circumference_batch_persistence.py`。测试负责人未修改业务代码。
- 执行命令及结果：
  - `python -m unittest discover -s tests -p "test_profile_macro_and_measurements.py" -v`：7 passed。
  - `python -m unittest discover -s tests -p "test_circumference_batch_persistence.py" -v`：7 passed。
  - `py_compile` 覆盖 `src` 与 `tests`：通过。
  - 隔离数据断言：`CARBS_KING_DATA_DIR` 实际解析目录与目标路径一致；A 无记录，B 含 `2026-07-27` 六项围度；通过。
- 真实 UI 视口与截图证据：本轮正式目录未进入业务页面，无法确认目标手机视口尺寸；阻断停在首次个人资料弹窗，因此无新增业务截图，不将“未看到白屏”当作通过。此前原始白屏截图/日志由 UI 验收记录保存。
- 端口与进程清理：验收前后检查 `8765`、`8766`、`18765`，无监听；本轮未保留 Flet/Python 预览进程。
- 以后如何提前拦截：在任何真实点击前先断言隔离目录和 `profile_inited=true`，准备 A/B 两套固定数据；启动日志首个异常必须记录；首次展开/收起、无历史/有历史、刷新各走一次；移动端验收同时检查并排标签高度/基线/宽高/间距、长中文和大字体换行、目标宽度不越界，并保留截图。若再次被初始化弹窗阻断，立即标记“环境阻断”，停止服务并报告唯一阻断证据及恢复路径，不连续尝试后台启动技巧。

## 网页 UI 预检环境阻断（2026-07-28）

- 阻断证据：正式目录 `D:\\carbs-king` 的 `127.0.0.1:8765` 未监听，无法建立网页预览连接，因此没有真实点击、页面截图或趋势刷新证据。
- 处理结果：未继续尝试其他后台启动方式；已确认历史预览端口/进程清理完成。本批次不再启动预览。
- 自动测试边界：已覆盖围度批量保存的全填、部分填、空值、非法值、保留旧值、历史兼容与整次删除，以及 `profile_circumference_expanded` 初次展开/收起和空/历史数据视图契约；专项单元测试与 `py_compile` 通过。自动测试不能替代 Flet/Web 点击，也不能替代 iQOO 11S 真机验收。
- 恢复条件：仅在项目经理重新确认后，按固定 `360x800`、竖屏、DPR 4（若支持）启动一次 Web 几何模拟预检；预检结果必须标注“iQOO 11S 几何模拟预检”，不得标记为真机通过。
