# Build 79 前台休息提示与应用内更新

日期：2026-07-31

## 交付内容

- 组间休息结束时，只要应用仍可见且可交互，就直接播放内置 `rest_coin` 音频；应用隐藏、暂停或锁屏时不走 Flet 播放器，继续由 Android `AlarmManager`/接收器负责。
- 手机“添加动作”列表固定单列并保留右侧 8px 间距；卡片高度增加，参数和选择按钮不再贴底。
- 手机“调整动作顺序”卡片使用独立外框，列表为完成状态的四行内容预留足够高度。
- 更新页可直接下载 APK，显示字节/百分比进度，校验 Release 的大小和 SHA-256；下载完成后通过 Flet 现有 FileProvider 打开系统安装界面，安装仍由用户确认。

## APK 验证

- 包名：`com.chenyang.carbs_king`
- versionName：`1.2.3`
- versionCode：`79`
- 大小：`201,667,039` 字节
- SHA-256：`26CF622FF2B6B3F0C310A27092C914DB6F3036EAC58C75B2F47322B2223374AE`
- v2 签名有效，证书 SHA-256 保持为 `172A8B5C7A909A79FB483F83CF9FEA71FE6567937C900C1D47EAF0FD67AD75CE`。
- 资源门禁：1324 GIF、1324 JPG、2 MP3、1 WAV；原生提醒与应用内安装 Provider 门禁均通过。
- 全量验证：444 项测试、441 个子测试通过。

## 真机验收

1. 在应用前台完成一组，等待组间倒计时自然结束，确认内置音频播放。
2. 切后台和锁屏后分别做一次自然到点测试。
3. 在“我 → 应用更新”下载下一次发布包，确认进度、校验失败提示和系统安装界面。
4. 在“添加动作”和“调整动作顺序”确认卡片宽度、底部间距和四边框。
