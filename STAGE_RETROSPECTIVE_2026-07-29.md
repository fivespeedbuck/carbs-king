# 碳水大王 2026-07-29 阶段复盘

## 1. 阶段结论

本阶段完成了前序错误清理、自动宏量公式修复、目标挑战收口、主页布局统一、资源构建门禁、APK 重建和 Git 分支整理。

最终交付基线：

- 分支：`codex/main-cleanup`
- 核心提交：`ca4f08c`、`b44934f`
- 全量测试：381 项通过
- APK：`build/apk/carbs_king.apk`
- 版本：`1.2.3 / Build 74`
- APK 大小：201,510,855 字节
- APK SHA-256：`A460541071FE27DD40962284F36B09A4A81E2BFAA0240E7E9AD2E7C5A08DBA7C`
- APK v2 签名证书 SHA-256：`172A8B5C7A909A79FB483F83CF9FEA71FE6567937C900C1D47EAF0FD67AD75CE`
- 下一构建号：75

## 2. Git 清理结果

前序提交 `38436b5 fix: enforce packaged asset completeness` 已经进入远端 `main`，不能用 reset 或 force-push 粗暴抹除。本阶段采用前向修正：

1. 从 `main` 创建 `codex/main-cleanup`。
2. 保留前序有效业务功能，不整包回退。
3. 从后续版本控制状态中删除 2653 个重复的 `src/assets/**` 文件。
4. 保留根目录 `assets/**` 作为唯一资源源。
5. 建立可复现的构建前镜像和 APK 内层资源校验。
6. 所有修复、测试和构建先在清理分支完成。
7. 用户明确授权后才推送并快进合并 `main`。

`main.py` 经过边界测试，当前没有证据表明它被塞入业务表单。它仍负责路由、控制器装配、运行时上下文和主页面容器，这是允许的基础职责。

## 3. 资源与 APK 根因

### 3.1 错误是怎样发生的

Flet 配置使用 `[tool.flet.app] path = "src"`，构建器从 `src/assets` 收集资源。前序处理把 `src/assets/exercises/**` 提交进 Git，但 `.gitattributes` 只正确覆盖根目录 `assets/exercises/**`。结果是：

- 文件名和数量看起来正确；
- `src/assets/exercises/**` 实际是约 130 字节的 Git LFS 指针文本；
- APK 内能找到 1324 个 `.gif` 文件名，但内容不是 GIF；
- 错误 APK 只有 71,915,571 字节，却被当作成功包上传到 `v1.2.3` Release。

这类问题不能用 APK 大小或 `Successfully built` 判断。必须打开 APK 外层 ZIP，再打开 `assets/flutter_assets/app/app.zip` 内层 ZIP，检查真实文件头、路径、数量、字节数和哈希。

### 3.2 最终正确方案

- `assets/**`：唯一真实资源源。
- `assets/exercises/**`：唯一训练媒体 LFS 路径。
- `src/assets/**`：构建前镜像、Git 忽略、不得长期维护第二份。
- `build_apk_update.ps1` 固定执行：
  1. `git lfs pull --include="assets/exercises/**"`
  2. `tools/asset_gate.py verify-source`
  3. 将根 `assets` 镜像到 `src/assets`
  4. `tools/asset_gate.py verify-mirror`
  5. Flet Android 构建
  6. `tools/asset_gate.py verify-apk`
  7. 全部通过后才递增下一构建号

### 3.3 最终资源基线

- 2653 个文件
- 155,783,546 字节
- 1324 GIF，文件头为 `GIF87a` 或 `GIF89a`
- 1324 JPG
- 2 MP3
- 1 WAV
- APK 内层 `app.zip`：131,739,284 字节
- 内层媒体数量和总字节与根资源完全一致

### 3.4 干净检出复现

在临时 clone 中验证过完整链路：

1. 跳过 LFS 时，样本 GIF 是 130 字节指针。
2. 执行 `git lfs pull` 后，样本恢复为 92,828 字节且文件头为 `GIF89a`。
3. 再执行资源同步、Flet 构建和 APK 内层检查。
4. 资源统计与正式目录一致。

## 4. 自动宏量公式

### 4.1 输入因素

自动模式会使用：

- 性别
- 年龄
- 身高
- 体重
- 体脂
- 运动习惯
- 碳循环目标（减脂、保持、增肌）
- 日型（高碳、中碳、低碳）

资料不完整时不计算；手动模式不会被自动模式覆盖；老用户迁移默认目标为减脂。

### 4.2 日目标热量系数

| 目标 | 高碳日 | 中碳日 | 低碳日 |
| --- | ---: | ---: | ---: |
| 减脂 | 0.90 × TDEE | 0.80 × TDEE | 0.70 × TDEE |
| 保持 | 1.10 × TDEE | 1.00 × TDEE | 0.90 × TDEE |
| 增肌 | 1.15 × TDEE | 1.05 × TDEE | 0.95 × TDEE |

### 4.3 三大营养素

- 蛋白质按去脂体重计算，界面乘数和实际基准一致。
- 脂肪按日目标热量比例计算：高碳 25%、中碳 30%、低碳 35%。
- 碳水补足蛋白质和脂肪后的剩余热量。
- 满足 `蛋白质×4 + 碳水×4 + 脂肪×9 = 日目标热量` 的闭合关系。
- 没有使用 `min()`、固定碳水上限或隐藏显示掩盖公式冲突。

回归样例覆盖男、61.5 kg、12.9% 体脂、175 cm、30 岁、高频训练，以及减脂/保持/增肌三目标的高/中/低碳日。

## 5. 目标挑战最终规则

### 5.1 进行中与统计

- 最多 3 个进行中挑战。
- 3 个挑战可以属于同一赛道。
- 所有模板继续在后台按真实记录统计。
- 只有用户主动列为挑战目标后，才进入可见的完成/失败确认流程。
- 身体目标已完成最高档时，从推荐中隐藏；身体记录反向变化后可以重新出现。
- 可重复训练、饮水和饮食挑战完成最高档后，可继续提升档位或周期。

### 5.2 完成流程

- 完成后不自动弹窗，不打断用户浏览。
- 点击“待确认完成”卡片后才打开成果窗口。
- 多项成果合并为一个窗口。
- 成果内容直接显示“完成了什么”，不显示“普通级待确认”等内部状态。
- 成功文案：`Yeah Buddy! Light Weight Baby!`
- 奖杯背景使用本批成果的最高等级色。

等级：

- 优秀：绿色
- 精良：蓝色
- 史诗：紫色
- 传说：橙色
- 精锐：红色

只有精锐和“精锐 +N”播放完整训练完成音频；传说及以下静音。

### 5.3 失败流程

- 失败不自动弹窗。
- 失败卡为灰色，点击后打开嘲讽窗口。
- 失败完全静音。
- 主按钮：`不服，编辑后重来`
- 次按钮：`先挂着丢人`
- 失败挑战可以预填原目标、动作和日期周期后重新挑战。
- 失败文案池包含：
  - `恭喜，你成功证明了计划不会自己完成。`
  - `计划写得挺狠，执行得挺软。`
  - `嘴硬没用，记录不会替你训练。`

### 5.4 自定义挑战

- 动作型挑战复用全屏动作库，不输入动作 ID。
- 用户看到并选择动作名称。
- 天数型挑战单位固定为“天”。
- 修复了非身体挑战方向错误：此前会保存成“不高于”，导致 0 进度直接判定完成；现在非身体挑战统一为“不低于”。

## 6. UI 收口

- 性别按钮中的 `?` 已恢复为“女”。
- 空挑战创建卡占满挑战面板宽度。
- 挑战完成/失败窗口内容自适应高度。
- 挑战详情弹窗使用紧凑内容高度，不再接近全屏留白。
- 挑战详情和失败窗口右上角关闭按钮均绑定真实关闭回调。
- 新建挑战“目标值”和“单位”采用统一双列布局；针对 Flet Web 下拉框实际渲染高度额外校准。
- 今日、训练、饮食、数据、我五个主页的大卡统一以数据页为宽度基准。
- 训练页额外处理了顶部标签、空状态、计划状态和完成总结各自独立的水平缩进。

网页以 360×800 逻辑视口验证，比例等同于 20:9。网页预览和 iQOO 11S 真机仍可能因设备像素密度、状态栏、安全区、系统字体和 Android 渲染产生轻微差异；最终验收以 APK 真机为准。

## 7. 测试与验证

最终全量测试：381 项通过。

覆盖范围包括：

- 自动宏量公式
- 目标挑战与完成/失败流程
- 精锐颜色与音频门槛
- Profile 与围度
- 饮食信息架构
- 训练计划和训练中状态
- 备份与恢复
- 每日记录持久化
- 资源门禁
- `main.py` 边界
- UI 控件尺寸和主页宽度

同时完成：

- 相关 `py_compile`
- `git diff --check`
- APK v2 签名校验
- 历史 APK 证书连续性比对
- APK 包名、versionCode、versionName 检查
- APK 内层资源检查

测试命令需要让 `src` 位于模块搜索路径。例如 PowerShell：

```powershell
$env:PYTHONPATH = "D:\carbs-king\src"
python -m unittest discover -s tests -p 'test_*.py' -v
```

如果忘记设置 `PYTHONPATH`，出现 `ModuleNotFoundError` 属于测试命令环境错误，不是代码失败。

## 8. 本阶段确认的前序失误

以下均有 Git、文件内容、APK 或运行截图作为证据：

1. 未遵守分支规则，直接向 `main` 推送 `38436b5`。
2. 把本应是生成物的 `src/assets/**` 重复提交进 Git。
3. `.gitattributes` 只覆盖根资源，实际构建目录中的文件仍是 LFS 指针。
4. 用指针资源构建约 71.9 MB APK，并上传覆盖 GitHub Release。
5. 只统计文件名和数量，没有检查文件头、真实字节和哈希。
6. 一度只检查 APK 外层 ZIP，没有进入 Flet 的内层 `app.zip`。
7. 把增肌高/中/低碳日全部写成 `1.10 × TDEE`，没有形成真实碳循环。
8. 剩余热量全部反推为碳水，出现 `碳 7.8`，却没有先修正整体能量模型。
9. 自定义非身体挑战保存了错误方向，导致 0 进度立即完成。
10. 性别按钮文案被源码直接写成 `?`，不是字体或编码问题。
11. 多个弹窗关闭按钮只有图标，没有可靠关闭回调。
12. 修改子模块后只刷新浏览器，没有重启不监听子模块的 Flet 预览进程，导致看到旧代码。
13. 多目录、临时 worktree 和重复负责人并行，造成正式目录与测试结论不一致。
14. 把命令错误、环境错误和代码失败混为一谈。
15. 长构建在前台工具超时后被误判为失败或停止，没有继续检查进程、日志和产物。

同时必须避免不实指控：当前没有证据表明 `main.py` 被塞入业务表单；边界测试证明它仍符合路由和运行基础职责。

## 9. 可复用方法

### 9.1 接管脏工作树

- 先读项目上下文和交接文档。
- 核对正式目录、分支、`git status --short`、diff、进程、端口和构建产物。
- 不对数千项未知修改执行 reset、checkout 或批量还原。
- 从当前事实创建修复分支，采用前向修正。
- 将命令错误、环境错误、代码失败分别记录。

### 9.2 本地预览

- 只在 `D:\carbs-king` 启动一个预览服务。
- 修改非入口 Python 模块后必须重启预览进程，不能只刷新网页。
- 使用独立测试数据目录，避免 UI 点击污染真实记录。
- 每次重启前清理旧 Flet/Python 进程和 `38765/8765/8766` 端口。
- 浏览器检查手机比例，APK 真机做最终验收。

### 9.3 Android 构建

- 使用 UTF-8 环境。
- 长任务用隐藏后台进程和日志。
- 工具等待超时不等于构建失败。
- 必须持续检查进程、日志、APK 修改时间和最终退出状态。
- “构建成功”只是中间条件，资源、签名和版本门禁才是交付条件。

### 9.4 Git 与发布

- APK 不提交 Git。
- 代码先在修复分支完成并测试。
- 用户确认后再推送、合并 `main` 或替换 Release。
- 禁止 force-push 和历史改写。
- 上传 Release 前后分别核对大小和 SHA-256。

## 10. 下一阶段：UI 视觉重做

用户已明确当前颜色体系观感不理想，下一阶段将整体重做 UI。建议先建立视觉令牌和组件规范，再逐页迁移：

1. 确定背景、卡片、主色、辅助色、警告色、成功色和文字层级。
2. 确定圆角、边框、阴影、间距和按钮状态。
3. 先做“今日”与底部导航作为视觉基线。
4. 再迁移训练、饮食、数据和我。
5. 保留挑战等级色的语义，不让全局主题破坏优秀/精良/史诗/传说/精锐辨识。
6. 每批继续使用真实手机比例和 APK 真机回归。

视觉重做不得回退本阶段已经确认的功能公式、挑战流程、资源门禁、主页宽度、数据持久化和测试契约。

## 11. Build 75 增量收口

- 数据页维度切换收进主卡，主页层级保持一致；旧浅绿色内容底板统一为浅灰。
- 挑战日期改为从创建日向后计算；三项上限在新建、推荐和重试入口统一提示。
- 建立四套人工主题色板，避免 Material 自动色板把页面背景和卡片整体染色。默认绿色，仅“我”页允许切换。
- 主题实现的关键经验：固定白色卡片、浅灰内容底板和语义色，只让主按钮、选中态、进度与强调图标使用主题令牌；不要把 `surface` 全面绑定主题种子。
- 身高和年龄采用失焦自动保存并显示底部提示；年龄以保存年份为基准，每年元旦递增一次，旧用户首次升级不追溯补算多年。
- Flet 修改子模块后必须重启预览进程；浏览器刷新可能继续使用旧服务会话。清理进程时应先按进程名限定，再匹配命令行，避免筛选条件误中执行清理的 PowerShell 自身。
- Build 75 为 201,518,971 字节，APK 内层资源 2653 个、155,783,546 字节，SHA-256 为 `765E3EA57233BDA6D497048B652F7DC294BE21F457D9E6B001D8B7158D80210F`。
