# 碳水大王：项目上下文

> 给新 Codex 会话的稳定项目记忆。先阅读本文件，再阅读 `CURRENT_HANDOFF.md`，随后以工作区、`git status` 与源码为准。不要把历史聊天当作当前事实。

## 项目身份

- 项目：碳水大王（`carbs_king`）
- Android 包名：`com.chenyang.carbs_king`
- 技术栈：Python + Flet `0.85.3`
- 工作目录：`D:\carbs-king`
- 目标用户：项目所有者本人，iQOO 11S；个人离线健身、饮食与恢复记录应用
- 产品定位：不是桌面 SaaS，也不是纯数据日记；重点是每天可执行、训练中可操作、每周可复盘。

## 当前版本与发布

- 当前发布线：`1.2.3 / Build 78`；完整阶段结果见 `CURRENT_HANDOFF.md` 和 `CHANGELOG.md`。
- `pyproject.toml` 与 `src/app_version.py` 当前准备下一次 Build 79；发布前必须读取 APK 内的真实 `versionCode`，不能只信文件名或配置号。
- GitHub Release `v1.2.3` 使用 Build 78 APK；本地发布基线 SHA-256 为 `BBD04D9961478E67917C7691AD9390A215EED31C8981D14A1CC75DBA3206E9E1`。
- 应用内更新首选 GitHub Releases API，遇到 403、网络错误、超时或无效 JSON 时自动读取根目录 `update_manifest.json`；发布 APK 时必须同步更新该清单。
- Android 更新必须沿用既有签名；先核验包名、版本、签名、ZIP 对齐、媒体资源数量，再交付。
- APK 不提交 Git；通过 GitHub Release 上传。动作媒体已使用 Git LFS，普通 Git 推送大媒体容易因代理出现 HTTP 408。
- 发布前必须同步 README、CHANGELOG、Release 更新说明、版本号、Build 与 APK 资产文件名。
- 没有用户明确同意，不推送、建 Release 或覆盖远端。

## 已形成的产品结构

- 一级页：今日、训练、饮食、数据、我。
- 今日页默认打开；日期/保存条刻意位于今日页底部，不要擅自上移。
- 六餐：早餐、午餐、晚餐、练前、练后、偷吃。
- 训练支持力量、计时、有氧、自重、超级组、复合组；训练时长属于训练会话，离开训练页、切后台或锁屏仍持续。
- 动作库：1324 条离线中文动作，含图片与 GIF；按部位、细分肌群、器械筛选，个人常练优先，其次基础热门，冷门变式靠后；详情/编辑页展示 GIF、器械、肌群与动作要点。
- 食物库：2000+ 离线条目，包含原始食材、家常菜、早餐、外卖/地方菜、便利店食品；搜索优先于分类，成菜营养值是估算值，允许调整份量。
- 数据页是周复盘中心：趋势、日历、汇总；缺失数据不能补零或伪造趋势。
- 成就系统仍在旧代码中；用户已明确觉得被动成就无聊，计划未来替换为主动目标系统，但该改造目前暂停，不能误报为完成。

## 用户体验硬约束

- 默认中文；移动端优先，接受标准是 iQOO 11S 逻辑尺寸与手机真机，不是桌面网页。
- 可读性优先于毛玻璃或装饰。正文不使用过小字号，标签与输入框分离，避免浮动标签压边。
- 默认主题为绿色；只在“我”页提供绿、紫、蓝、黄四个横向色块。主题只改变主按钮、选中态、进度和强调元素，白色卡片、浅灰内容底板及错误/等级语义色保持稳定。
- 并排控件必须固定标签高度、统一基线、等宽/间距；输入框长文本不能把同排控件顶歪。
- 所有表单字段多时优先全屏表单：固定顶栏、内容滚动、底部固定保存；简单确认才用小弹窗。
- 默认不展示右侧长滚动条，但页面必须仍能触摸滚动到底。
- 训练中黑色专注页不能因内容类型而随机溢出或重排；力量、有氧、计时、超级组、复合组与休息态均要按真实手机高度验收。
- 训练卡操作区不能压缩动作内容。训练中的“当前动作”要比重量/次数更醒目。
- 组间休息：休息卡必须与训练卡匹配高度；标题和倒计时在顶部至按钮区域之间居中，控制按钮与下一训练项目顺序由当前界面规格决定，修改前先看现有代码/截图。
- 动作库、食物库的筛选控件不可越界；切换筛选不得把横向滚动视口强制跳回开头。
- 动作库用户可见名称使用中文健身房常用叫法，不展示英文动作名；精确主名和常用别名优先，冷门变式至少关联基础动作搜索词。文本搜索无严格筛选结果时可逐级放宽肌群、器械和部位，但必须显示放宽提示。
- 已成功安排的休息 `AlarmManager`/进程内定时器是唯一到点声音交付者；前台 tick 只持久化完成状态。暂停、调整、跳过仍必须取消或重排原生提醒。

## 协作与验证规则

- 用户会连续补充需求：非阻断需求先入当前批次队列，当前批次结束后统一开发、统一测试；崩溃、数据丢失、白屏属于 P0，可中断。
- 产品经理角色负责对接、拆批、汇总；开发、测试可分工，但同一文件要明确唯一写入者，避免覆盖。
- 不要让已完成 agent 常驻；不要为了小修改反复开关多 agent。
- 主动报告阶段进度。用户反感“无反馈、黑盒等待、反复开预览服务”。
- 每次重新启动网页预览前，关闭项目旧预览服务及其 Python 子进程；不保留 `8765/8766` 等历史测试进程。除非用户明确要求，不控制鼠标或制造可见窗口。
- Flet 真实网页事件与单元测试可能不同：下拉选择用 `on_select`；关键弹窗和筛选必须做至少一次真实交互验证。
- 写入型测试必须显式设置并断言 `CARBS_KING_DATA_DIR`，避免污染真实记录。
- 全量测试、`py_compile`、差异格式检查按风险执行；Android 构建期间不要并行跑大测试抢资源。

## 本机技术约束

- Windows / PowerShell；脚本执行策略拦构建时，用进程级 `powershell -ExecutionPolicy Bypass -File ...`，不要改全局策略。
- Flet 本机启动使用 `flet run`，不要使用 `python -m flet`。
- 当前 Flet 版本存在部分新式控件/枚举兼容问题。优先沿用项目已验证的基础控件写法，避免未经验证的 `Stack`、新枚举或复杂圆角构造直接进入主界面。
- 根目录 `assets` 需要在 APK 构建前镜像到 `src/assets`；发布前检查 APK 内动作 GIF、图片与提示音确实存在。
- Kimi 开放平台 Key 只用于用户自己的 VS Code/Continue；用户明确要求本项目和 agent 不再调用 Kimi API。不要读取、输出或使用任何 API Key。

## 历史决策，仍然有效

- 旧 106 力量动作已被 1324 动作库替代；用户尚未正式依赖旧动作数据。
- 动作媒体全部离线内置，包体变大是主动取舍。
- 食物库不使用图片，避免无意义增大 APK。
- 自定义动作需选择训练部位、目标肌群、器械，并写入对应筛选分类。
- 补剂库属于摄入/恢复管理；入口应去重，避免同页重复“新增补剂/补剂库”。
- 围度只保留胸、腰、臀、上臂、大腿、小腿；不要扩展低价值的颈围、肩围。
- 发布素材不能使用用户带标注的验收截图；正式素材必须是隔离演示数据生成的干净截图。

## 新会话启动清单

1. 阅读本文件与 `CURRENT_HANDOFF.md`。
2. 运行 `git status --short`，阅读当前 diff；不得假设工作区干净。
3. 核对 `README.md`、`CHANGELOG.md`、`pyproject.toml` 与远端 Release，确定当前发布线。
4. 先报告当前正在做什么、哪些未验证，再决定是否继续开发、测试、打包或发布。
5. 只在当前任务完成并经用户确认后才构建 APK / 发布 GitHub。
