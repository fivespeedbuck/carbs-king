# 待实现功能清单

本文档记录 Build 89 后仍待实现、待深化或待继续观察的功能。

## 1. 后台组间休息提醒与悬浮倒计时

### 目标

训练进行组间休息时，即使应用切到后台或锁屏，也能持续显示可拖动的悬浮倒计时；倒计时结束后以铃声、震动和高优先级通知提醒用户。

### 当前状态

Build 88 已实现并在 iQOO 11S 验证应用前台、切后台、锁屏、息屏提醒，以及悬浮窗显示、拖动、点击返回、下一动作、主题色和调整时间复用通知。

### 后续边界

- Android“强行停止”后无法继续提醒；手机重启后的未完成倒计时恢复尚未实现。
- 后续仅在真实需要时补 OriginOS 自启动/电池策略诊断，不再重做当前已通过链路。

### 已验收范围

- iQOO 11S 已验证前台、切后台、锁屏、息屏、修改时间、悬浮窗拖动和返回应用。
- 手动划掉应用与系统“强行停止”不是同一种状态；后者由 Android 明确禁止后台能力。

## 2. 已完成训练记录回收站

### 目标

删除“当日已练”的训练记录时，先放入回收站；可恢复至原日期的“当日已练”；仅在回收站内再次删除才永久移除。

### 当前状态

Build 88 已实现：删除完成训练后进入回收站，保留 15 天；入口位于“我 → 功能设置”；可恢复到原日期的“当日已练”或彻底删除；数据纳入完整备份。自动化测试通过，后续使用中继续观察历史日期恢复体验。

### 已实现范围

- 独立持久化回收站数据，并纳入导入/导出备份。
- 在“我 → 功能设置”提供与现有卡片一致的入口。
- 展示原训练日期、训练内容、删除时间。
- 恢复时追加到对应日期的已完成训练集合，不覆盖同日已有训练。
- 回收站二次删除永久清理。

### 验收标准

- 删除、恢复、同日已有训练、历史日期恢复、永久删除、备份还原均通过。

## 3. “我”页体重/体脂同步到数据趋势

### 目标

用户在“我”页修改并保存体重或体脂后，自动写入当天的数据趋势；不需要再到“数据”页重复录入。

### 当前状态

Build 89 已实现并通过 iQOO 11S 真机验收。资料页实际修改体重或体脂后，会把改动项写入当天明确测量，并保留同日已记录的另一项；首次完成个人资料也会生成当天测量点。数据页仍负责历史日期补录。

### 需要实现

- 仅在体重或体脂发生实际变化并保存时，写入当天测量记录。
- 只改一个指标时，保留当天另一指标的有效显式测量值。
- 保留“数据”页按历史日期补录的能力。

### 验收标准

- 首次资料保存、单独改体重、单独改体脂、同日两次编辑、无效输入和历史补录均正确。

## 4. 饮食页并排控件与下拉菜单

### 目标

“添加饮食”页面的餐次下拉框/数量输入框，以及搜索食物输入框/食物下拉框，在 iQOO 11S 上拥有一致的可见外框高度和基线。

### 当前状态

Build 89 已放弃 OriginOS 上不可靠的原生透明下拉菜单，改为从底部向上展开的不透明白色应用内选择面板，并为最后一项保留底部安全空间；iQOO 11S 真机验收通过。

### 验收标准

- 先对照 Build 76 与 Flet Android 实际渲染，定位内部 TextField/Dropdown、标签容器或主题样式的实际尺寸来源。
- 在未聚焦、键盘弹出、下拉选项选择后均保持视觉对齐，并以真机截图确认，不以源码参数或单元测试代替。

## 5. 动态碳循环（Build 90 正式候选）

动态碳循环的唯一计算规范是 Obsidian 的 `[[02-项目/碳水大王/动态碳循环计算系统]]`；100 天基线和派生缺失场景见 `[[02-项目/碳水大王/动态碳循环-100天虚拟数据回放]]`。本文件不再维护第二套公式、阈值或 UI 规则。

### 当前完成状态（2026-08-02）

- 已完成计算与接入层：`src/dynamic_carb_engine.py` 唯一计算内核、`src/dynamic_carb_adapter.py` 数据适配与快照、训练 schema v3、参数完整/自重门槛、分类器、宏量求解、手动分层、UI 最小投影、31/60/90 天 Hall shadow 校准。
- 已完成回放：100 天 golden、12 类×365 日、96 用户/35040 用户日压力回放、18 项不可协商边界；当前均 0 硬错误。
- 已完成 App 原始数据适配、不可变展示快照、可重算实际账本和首页/训练/饮食三页接线；完整回归 489 项测试、443 个子测试通过。
- 自动宏量模式才调用动态引擎；自定义宏量模式不得生成或应用动态目标，用户倍率为最终值。
- shadow 校准可以随测试版运行和保存，但真实手机备份回放前不得自动应用。

已废止：固定训练部位系数、大一统负荷分、`0–6 / 7–14 / 15+` 判档、固定 7/31 天碳水修正、习惯训练时间、训练前两小时分支，以及按当天已摄入量追赶或倒扣目标。

剩余执行顺序：

1. 用户真机验收 Build 90 候选后再决定是否发布，验收前不更新 Release；
2. 持续积累真实饮食、体重和训练备份，时间顺序回放后再讨论受限自动能量校准；
3. Build 90 完成后单独测量启动性能，只清理确认无引用且确实带来负担的旧路径。

训练结束状态评分和休息建议不属于本升级；独立规范见 Obsidian 的 `[[02-项目/碳水大王/训练恢复与休息建议系统]]`。动态碳循环只消费用户最终确认的休息或训练事实，不读取状态分直接调碳水。

## 6. 训练恢复与休息建议（独立未来扩展）

当前只预留每场训练可跳过的 5 点状态字段，不在动态碳循环 P0 中实现恢复算法。以后单独按“只采集 → 后台回放 → 保守建议 → 受限个体化”的顺序开发；系统只建议降量/休息，不能自动替用户改计划或碳档。

## 7. 新增固定轨迹器械动作的资源纳入与交付验证

### 当前状态

Build 88 继续包含“站姿器械下夹胸”和“站姿器械侧平举”及其 JPG、双帧 GIF。后续新增动作沿用同一资源门禁流程。

### 下次执行

- 新增动作时同步纳入数据、JPG、GIF 和搜索测试。
- Git 跟踪媒体后执行资源门禁，并在 APK 构建后确认素材进入安装包。
