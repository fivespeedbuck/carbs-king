# 待实现功能清单

本文档只记录用户已经提出、但当前 Build 82 基线尚未交付或未完成真机验收的功能。它们均不应被表述为“已实现”或“已在 iQOO 11S 验收通过”。

## 1. 后台组间休息提醒与悬浮倒计时

### 目标

训练进行组间休息时，即使应用切到后台或锁屏，也能持续显示可拖动的悬浮倒计时；倒计时结束后以铃声、震动和高优先级通知提醒用户。

### 当前状态

未实现。此前 Kimi K3 工作区曾存在未提交的尝试代码，已按要求清理，不能视为功能交付。

### 需要实现

- Android 原生前台服务，负责休息倒计时生命周期。
- 悬浮窗服务与可拖动的小型倒计时视图。
- 精确闹钟/原生广播接收器，到点触发声音、震动和通知。
- 应用内逐项检查并引导：通知、精确闹钟、悬浮窗、自启动、后台弹出、锁屏通知/亮屏等权限。
- 对 iQOO / OriginOS 的兼容说明和失败状态提示。

### 验收标准

- iQOO 11S 上分别验证：前台、切到后台、锁屏、手动划掉应用。
- 验证倒计时显示、到点声音、震动、通知和恢复应用后的状态一致性。
- 仅在上述实机场景通过后，才可标记为已交付。

## 2. 已完成训练记录回收站

### 目标

删除“当日已练”的训练记录时，先放入回收站；可恢复至原日期的“当日已练”；仅在回收站内再次删除才永久移除。

### 当前状态

未实现。此前未提交版本已清理，当前 Build 82 不包含回收站存储、入口或恢复逻辑。

### 需要实现

- 独立持久化回收站数据，并纳入导入/导出备份。
- 在“我 → 功能设置”提供与现有卡片一致的入口。
- 展示原训练日期、训练内容、删除时间。
- 恢复时追加到对应日期的已完成训练集合，不覆盖同日已有训练。
- 回收站二次删除永久清理。

### 验收标准

- 删除、恢复、同日已有训练、历史日期恢复、永久删除、备份还原均通过。

## 3. “我”页体重/体脂同步到数据趋势

### 目标

用户在“我”页修改并保存体重或体脂后，自动写入当天的数据趋势；不需要再到“数据”页重复录入。

### 当前状态

未实现。当前 Build 82 中“我”页资料与带测量时间戳的趋势记录仍是两条独立数据路径。

### 需要实现

- 仅在体重或体脂发生实际变化并保存时，写入当天测量记录。
- 只改一个指标时，保留当天另一指标的有效显式测量值。
- 保留“数据”页按历史日期补录的能力。

### 验收标准

- 首次资料保存、单独改体重、单独改体脂、同日两次编辑、无效输入和历史补录均正确。

## 4. 饮食页并排控件视觉对齐

### 目标

“添加饮食”页面的餐次下拉框/数量输入框，以及搜索食物输入框/食物下拉框，在 iQOO 11S 上拥有一致的可见外框高度和基线。

### 当前状态

此前针对外层高度与内部字段高度的调整已进入 Build 82，自动化测试通过，但 iQOO 11S 真机视觉效果没有变化；问题仍未解决，不能标记为已交付。

### 验收标准

- 先对照 Build 76 与 Flet Android 实际渲染，定位内部 TextField/Dropdown、标签容器或主题样式的实际尺寸来源。
- 在未聚焦、键盘弹出、下拉选项选择后均保持视觉对齐，并以真机截图确认，不以源码参数或单元测试代替。

## 5. 新增固定轨迹器械动作的资源纳入与交付验证

### 当前状态

Build 82 已将“站姿器械下夹胸”和“站姿器械侧平举”及其 JPG、双帧 GIF 纳入 APK。后续新增动作沿用同一资源门禁流程。

### 下次执行

- 新增动作时同步纳入数据、JPG、GIF 和搜索测试。
- Git 跟踪媒体后执行资源门禁，并在 APK 构建后确认素材进入安装包。
